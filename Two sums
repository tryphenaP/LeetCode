class Solution(object):
    def twoSum(self, nums, target):
        
     value_to_index = {}

     for i , n in enumerate(nums):
       pair = target - n

       if pair in value_to_index:
             return ([value_to_index[pair], i ])
             break
       value_to_index[n] = i
